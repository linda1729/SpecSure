启动后端：`uvicorn backend.app.main:app --reload --port 8000`

启动前端：进到mockfrontend文件夹，`python -m http.server 5500`

访问网页：`http://localhost:5500/?api=http://8.140.214.49:8000`，我开了云服务器，所以直接上公网了，后续交接会改成本地的。

目前做到的工作是：

1、数据导入、预处理(如降噪、波段选择、标准化)

- [x] 选择默认数据（实现且验证）
- [ ] 自主上传数据（实现未验证）
- [ ] 预处理（实现未可视化）

2、分类模块(支持两种模型对比)

- [x] CNN（实现且验证）
- [ ] SVM（未实现）

3、分类结果可视化(伪彩色、分类图、标注对比)

- [ ] 伪彩色（？）

- [x] 分类图（实现且验证）
- [ ] 标注对比（？）

4、性能评估(准确率、Kappa、混淆矩阵)

- [ ] （均实现但没有单列界面，下一步就完善）

