- backend/app/core/config.py：移除自定义数据集注册，固定仅使用内置 IP/SA/PU。
- backend/app/services/cnn_service.py、backend/app/services/svm_service.py：删除数据集上传接口，启动前校验 data/ 目录就绪，推理模式同样解析报告并使用默认输出路径；推理时若路径不存在会自动匹配该数据集最新模型并返回对应 PCA URL。
- models/cnn/code/HybridSN/utils.py、models/svm/code/SVM/utils.py、models/svm/code/SVM/train.py：收紧数据加载到预置数据集并修正默认数据根，避免依赖上传/自定义路径。
- mockfrontend/index.html、mockfrontend/app.js：移除上传表单与事件，改为手动放置数据+重新检测的流程提示。
- mockfrontend/app.js：默认推理模型路径改为使用反斜杠分隔，便于直接复制到需要 Windows 风格路径的场景。
- mockfrontend/index.html：文案说明推理可留空，后台自动匹配最新模型。
- backend/API.md、backend/API_FULL.md：文档同步说明取消上传接口、推理可返回指标。
- 补充：CNN/SVM 推理路径解析时会兼容反斜杠，若未找到用户传入路径会自动回退到该数据集最新训练模型，避免路径格式导致的推理失败；前端默认推理路径会优先使用已生成产物列表中的最新模型。
- 新增：前端默认模型路径加入显式提示文件（PU=epochs10 的 CNN，SA=epochs100 的 CNN；SVM 为 IP/PU/SA 100epoch），与现有产物对齐，防止默认参数错配。
- 补充：增加 CNN IP 默认模型提示 `IndianPines_model_pca=30_window=25_lr=0.001_epochs=1.pth`，避免回退使用错误的 `.pca.pkl` 结尾。
- 优化：前端选择默认模型时会过滤掉 `.pca.pkl`，只会自动填充 `.pth` 与 `.joblib` 正式模型文件，避免拿到 PCA 文件。
- 细节：数据集切换或初始化时自动刷新默认模型提示（placeholder），防止推理默认路径与实际训练好的 epochs 不一致。
